apiVersion: v1
kind: Service
metadata:
  name: helloweb
  namespace: hello-namespace
  labels:
    app: hello
spec:
  selector:
    app: hello
    tier: web
  ports:
  - port: 8009
    targetPort: 8080
  type: LoadBalancer
